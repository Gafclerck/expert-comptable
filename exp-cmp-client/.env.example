# URL du backend FastAPI (serveur de dev + client HTTP). Montage des routes
# sous /api (settings.API_STR) : le suffixe /api est indispensable, sinon les
# appels du front partent sur /assistant/* au lieu de /api/assistant/*.
VITE_API_URL=http://localhost:8000/api